[
  { "name": "/nowplaying has an empty state", "steps": [
    { "send": { "text": "/nowplaying" }, "expect": [{ "method": "sendMessage", "payload": { "text": "Nothing is playing yet — tap Play music to choose a track." } }] }
  ] }
]
