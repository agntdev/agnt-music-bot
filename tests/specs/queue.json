[
  { "name": "/queue has an inviting empty state", "steps": [
    { "send": { "text": "/queue" }, "expect": [{ "method": "sendMessage", "payload": { "text": "No tracks are waiting — use Play music to add one." } }] }
  ] }
]
