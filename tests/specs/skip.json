[
  { "name": "/skip stops when the queue is empty", "steps": [
    { "send": { "text": "/skip" }, "expect": [{ "method": "sendMessage", "payload": { "text": "The queue is empty, so I stopped playback." } }] }
  ] }
]
