[
  { "name": "/resume handles no paused track", "steps": [
    { "send": { "text": "/resume" }, "expect": [{ "method": "sendMessage", "payload": { "text": "There isn't a paused track to resume." } }] }
  ] }
]
